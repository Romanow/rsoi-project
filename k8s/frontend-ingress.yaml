apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: qrook-frontend-ingress-service
  labels:
    app.kubernetes.io/name: qrook-frontend-ingress-service
    app.kubernetes.io/version: "3.0"
    app.kubernetes.io/component: application
    app.kubernetes.io/part-of: micro-services
    app.kubernetes.io/managed-by: helm
spec:
#{{/*  ingressClassName: nginx*/}}
  rules:
    - host: kurush7.cloud.okteto.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: qrook-frontend-service
                port:
                  number: 8080